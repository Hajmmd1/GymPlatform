# GymPlatform - Agent Configuration

Commands for lint, typecheck, and test (to be populated as codebase grows):

```json
{
  "lint": "npm run lint",
  "typecheck": "npm run typecheck",
  "test": "npm test"
}
```